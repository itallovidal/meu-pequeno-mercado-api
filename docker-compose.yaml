version: "3.18"

services:
  mercadinho-database:
    image: postgres
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USERNAME=postgres
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DATABASE=mercadinho-db